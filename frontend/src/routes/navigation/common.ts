export enum CommonNav {
  HOME = "/dashboard",
  UPDATE_USER = "atualizar",
  CLIENTE = "/cliente",
  AUDITOR = "/auditor",
  ADMIN = "/administrador",
  CLASSIFICADOR = "/classificador",
  SUPERVISOR_BASE = "/supervisor-base",
  SUPERVISOR_REGIONAL = "/supervisor-regional",
  LOGIN = "/"
}
